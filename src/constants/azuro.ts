/** Featured / tier cap — keep in sync with curation limits. */
export const MAX_FOOTBALL_MARKETS = 9;

/** After curation ordering, how many Azuro-shaped rows we return to clients (pool for lists/admin). */
export const AZURO_FEED_LIST_CAP = 72;
